repo: db-crm/david_bolivar_portfolio
branch: main

## Last sync
date: 2026-07-26T01:39:13Z

### Updated in this project
- Checked repo for a new FloodReady clip the user uploaded via GitHub; binary video couldn't be pulled through the copy tool, so the user attached it directly instead (saved to media/floodready-digital-twin.mp4).
- Rebuilt the portfolio as a story-first page: hero opens on how David works, not a job title, with a verification-thread strip.
- Selected work is now an overview grid of cards that expand in place; case-study media lazy-loads only on expand.
- FloodReady ENC promoted to a full case study; the Engineering note folded into CRM Central; personal projects replaced by "What I'm exploring."
- Dashboard carousel is its own always-visible section with per-report captions (Account Actuals and Delinquency captioned as pages of one report).

## Screen map
| Screen | Built from |
| --- | --- |
| Portfolio.dc.html | index.html (all sections, tokens, both inline SVG diagrams, typing + carousel scripts) |
| media/*.png (9 dashboard screenshots) | media/ |
